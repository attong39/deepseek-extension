# AI Per-file Upgrade Recommendations

This report lists prioritized, actionable recommendations for each file (first 120 files scanned) across Python and TypeScript.

Legend: [P1]=High impact, [P2]=Medium, [P3]=Low.

## Summary
- Focus: Fix Python parse errors, enforce DI and RBAC in backend, standardize imports in TS, and improve typing.
- Suggested workflow: Apply top items per folder, then re-run analyzer.

## Python Backend (apps/backend)

- app/middleware/security.py [P1]
  - Issue: libcst parse error at import lines. Likely broken indentation or stray characters.
  - Action: Reformat file; ensure imports are top-level; run ruff and black; add tests for middleware behavior.

- core/memory/backend_factory.py [P1]
  - Issue: tokenizer error: no matching outer block for dedent.
  - Action: Fix indentation; ensure from __future__ import annotations is the very first statement.

- core/observability/enhanced_monitoring.py [P2]
  - Issue: tokenizer error.
  - Action: Fix indentation and block closures; add minimal unit tests for exported functions.

- core/optimization/memory_manager.py [P2]
  - Issue: tokenizer error.
  - Action: Repair blocks; add type hints; validate with mypy.

- data/services/bulk_memory_adapter.py [P1]
  - Issue: parser error near concurrent.futures import.
  - Action: Remove stray tokens before imports; add thread-safety notes; limit worker pool via config.

- infra/vector_backends/pgvector_pool_backend.py [P2]
  - Issue: parse error near typing imports.
  - Action: Fix imports; add connection pool limits; error handling for DB timeouts.

- infrastructure/_scaffold/repository_base.py [P1]
  - Issue: parse error near UUID import.
  - Action: Validate base class definition; ensure ABC/Generic usage; add docstrings for CRUD contract.

- tests/** (multiple) [P2]
  - Issue: various tokenizer/parser errors.
  - Action: Normalize fixtures; ensure valid pytest imports; add minimal smoke tests that import each module.

## DI & Router Architecture (Apply patches)

- Create API v1 aggregator: `zeta_vn/app/api/v1/__init__.py` exporting `router`.
- Refactor `agents_example.py` and `plans_example.py` to use Depends for repos/use-cases; remove imports inside handlers; add tags ["v1", ...].
- Add factories in `zeta_vn/app/dependencies.py`: `get_agent_repository`, `get_plan_repository`, `get_audit_service`.

## TypeScript (extension, apps/desktop)

- Enforce import order and unused removal [P2]
  - Run `npm run imports:ts:fix`.

- Strengthen types on API boundaries [P2]
  - Add zod/io-ts validation for external inputs; narrow any to specific types.

- Performance [P3]
  - Memoize heavy selectors/hooks; lazy-load large panes.

## Automation

- Run dry-run codemods first:
  - `npm run ai:optimize:per-file`
- Then apply safely when green:
  - `npm run ai:optimize:per-file:commit`
- Re-run analysis:
  - `npm run ai:project:analyze && npm run ai:project:recommend`

---
Generated by AI project analyzer per-file pass.

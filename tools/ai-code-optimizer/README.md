# AI Code Optimizer

Minimal initial version integrating three building blocks:

- Import Optimization (libCST based)
- Duplicate Detection (hash-based chunks)
- Structure Enforcement (libCST based)

Configure via a YAML file next to this package (e.g., `config.yml`).

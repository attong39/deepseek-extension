"""Test Prometheus module."""

try:
    import prometheus_client

    print("prometheus_client available")
except ImportError:
    print("prometheus_client not available")
import ImportError
import print

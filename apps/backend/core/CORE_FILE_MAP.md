# CORE: File map and short descriptions

This file is an autogenerated, human-friendly map of `zeta_vn/core` important modules and a short description of each one to help onboarding and Copilot scaffolding.

Root: `e:/zeta/zeta_vn/core`

Tree (top-level folders shown with representative files):

- adapters/
  - `inmemory_metrics.py` — lightweight in-memory `Metrics` adapter used for tests and smoke runs.
  - `inmemory_alerts.py` — in-memory `AlertSystem` adapter for tests.
  - `asr_whisper.py` — adapter placeholder for Whisper ASR integration.

- performance/
  - `advanced_caching.py` — `AdaptiveCacheManager`: strategy selection for cache backends (redis/local/memcached), used by caching services.
  - `optimizer.py` — `AdaptivePerformanceOptimizer`: detect bottlenecks + apply optimization strategies (memory/cpu/network). Core-only, calls ports for metrics.
  - `instrumentation.py` — `instrument` decorator: traces and records metrics for synchronous use-cases.

- services/
  - `chat_runtime.py` — thin wrapper to time chat generation streams and emit latency metrics.
  - `health_monitor.py` — `RealTimeHealthMonitor` (sync) and `HealthMonitor` (async loop): collects health snapshots, triggers alerts/heal.
  - `performance_optimizer.py` — service wiring around `AdaptivePerformanceOptimizer` for application usage.
  - `disaster_recovery.py` (in `security/`) — manager that orchestrates backup/restore using `BackupService` and alerts.

  Expanded: `services/chat/` (detailed)
  - `conversation.py` — Conversation helpers and small facades used by higher-level chat flows; extension point for conversation-level utilities (threading, summarization, metadata normalization).
  - `service.py` — Thin package re-export that exposes `ChatService` at `zeta_vn.core.services.chat.service.ChatService` for DI and imports; keeps public API stable.
  - `_impl.py` — Actual `ChatService` implementation: async business logic for chat CRUD, message append, listing/search, statistics, export and bulk operations. Depends on repository ports + UoW factory; suitable for unit tests with fake repos.

  Other notable `services/` files (short descriptions)
  - `chat_service.py` — historical/top-level import path for ChatService; may re-export or delegate to `chat/_impl.py`.
  - `chat_runtime.py` — runtime wrapper for streaming LLM/chat generation (timing + metrics). Used by chat flow orchestration.
  - `caching_service.py` — service layer that coordinates cache backends, uses `AdaptiveCacheManager` from `performance/`.
  - `backup_service.py` — App-level orchestration for taking snapshots via `BackupService` adapter and storing metadata.
  - `memory_service.py` / `memory_manager_service.py` — memory management entrypoints; coordinate `AdaptiveMemoryManager` (phase 3 roadmap).
  - `performance_optimizer.py` — glue between runtime scheduler and `AdaptivePerformanceOptimizer` (calls `auto_optimize()` on triggers).
  - `scheduler.py` — lightweight scheduler helpers used by various services to start periodic background work.
  - `telemetry.py` — telemetry bridge wiring metrics/tracing adapters into services.


- observability/
  - `tracing.py` — small wrapper: uses OpenTelemetry tracer if available, otherwise provides a dummy tracer.
  - `metrics.py` — project metrics port/adapters (see `adapters/inmemory_metrics.py`).
  - `logging.py` — logging configuration utilities.

- ports/
  - `observability.py` — Protocols: `DistributedTracer`, `Metrics` (incr/gauge/timing_ms).
  - `feature_toggles.py` — Protocol `FeatureToggleProvider`.
  - `backup.py` — Protocol `BackupService`.
  - `advanced_alerts.py` — Protocol `AIAlertSystem`.
  - `documentation.py` — Protocol `APIDocumenter`.
  - `alerts.py` — `AlertSystem` protocol for simple alerts (info/warn/critical/page_oncall).

- mlops/
  - `manager.py` — `MLOpsManager` stub: auto-retrain + register + deploy workflow (emits metrics).

- security/
  - `disaster_recovery.py` — DisasterRecoveryManager: backup/restore orchestration (calls `BackupService` + `AIAlertSystem`).
  - `advanced_monitoring.py` — AI-driven monitoring hooks (predictions -> alerting).

- self_improvement/
  - `feature_rollout.py` — `feature_required` decorator to gate functions/use-cases with `FeatureToggleProvider`.

- testing/
  - test helpers and protocol definitions used in core tests (see `tests/core/`).

Notes & guidance

- Contract style: core modules depend only on `zeta_vn.core.ports.*` Protocols and receive adapters via constructor DI. Avoid importing `app` or `data` directly from `core`.
- File responsibilities:
  - ports/*: declare Protocols (stable contracts). No business logic.
  - adapters/*: lightweight in-memory adapters or external adapters that implement ports.
  - services/*: application-level workflows implemented in core (no infra calls); safe to unit-test with fakes.
  - performance/*: optimization logic (e.g., adaptive caching, performance tuning strategies).

Testing

- Use `zeta_vn.data.external.inmemory_*` adapters in unit tests to avoid infra.
- Example: `AdaptivePerformanceOptimizer` tests create a `FakeMetrics` and `FakeDetector` and assert `auto_optimize()` applies strategies and emits metrics.

How to update this map

- This file is intended as a living document. Run `tools/autobarrel_python.py` after adding new modules and update this file if new top-level modules are added.

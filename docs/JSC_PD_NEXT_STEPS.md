# JSC_PD / check_duplicates.py — Next automated steps

Tài liệu này đưa ra các đề xuất hành động tự động an toàn sau khi đã chạy `ruff` và chỉnh sửa `scripts/check_duplicates.py`.

Mục tiêu: hoàn thiện thay đổi, chạy kiểm tra kiểu và unit tests, và mở PR để review.

Đề xuất (tuần tự, PowerShell):

1. Kiểm tra trạng thái git và tạo branch để commit các thay đổi hiện tại

```powershell
# từ thư mục e:\zeta
git status --porcelain
git checkout -b fix/dup-check-jscpd-diagnostics
git add scripts/check_duplicates.py .githooks/pre-push .githooks/README.md
git commit -m "chore: improve jscpd diagnostics and make pre-push warning-only"
```

2. Chạy type checks (mypy). Nếu repo dùng mypy config, nó sẽ báo lỗi cần fix.

```powershell
# trong virtualenv (.venv)
.\.venv\Scripts\python.exe -m pip install -r requirements-dev.txt || echo "skip deps install"
.\.venv\Scripts\python.exe -m mypy zeta_vn || Write-Host "mypy returned non-zero"
```

3. Chạy unit tests (pytest)

```powershell
.\.venv\Scripts\python.exe -m pytest -q
```

4. Kiểm tra và chạy lại script duplicate check (smoke test)

```powershell
.\.venv\Scripts\python.exe .\scripts\check_duplicates.py --jscpd-only --report-dir .\reports\duplicates
```

5. Tạo PR draft (tùy bạn dùng GitHub CLI hoặc web UI). Ví dụ dùng gh cli:

```powershell
gh pr create --fill --title "chore: improve duplicate checks diagnostics" --draft
```

6. Cleanups & follow-ups (manual / opt-in):

- Nếu bạn muốn loại bỏ `# ruff: noqa: C901` trong `scripts/check_duplicates.py`, cân nhắc tách thêm các helper hoặc cập nhật cấu hình Ruff (per-file-ignores) trong `pyproject.toml`.
- Nếu muốn, mình có thể tạo patch tách thêm hàm để loại bỏ noqa; yêu cầu tiếp theo sẽ thực hiện.

Ghi chú

- Mình không tự chạy `git commit` hay `gh pr create` vì không tự động thao tác repo remote; mình chỉ tạo file này với các lệnh rõ ràng để bạn hoặc CI chạy.
- Nếu muốn, mình có thể tự động tạo thêm một branch patch file (chỉ trong workspace) hoặc tách helpers tiếp để loại bỏ noqa — hãy cho biết nếu muốn mình thực hiện tiếp.

---
Generated by automated assistant to summarize safe next steps after Ruff fixes and jscpd diagnostics improvements.
# JSC_PD / check_duplicates.py — Next automated steps

Tài liệu này đưa ra các đề xuất hành động tự động an toàn sau khi đã chạy `ruff` và chỉnh sửa `scripts/check_duplicates.py`.

Mục tiêu: hoàn thiện thay đổi, chạy kiểm tra kiểu và unit tests, và mở PR để review.

Đề xuất (tuần tự, PowerShell):

1) Kiểm tra trạng thái git và tạo branch để commit các thay đổi hiện tại

```powershell
# từ thư mục e:\zeta
git status --porcelain
git checkout -b fix/dup-check-jscpd-diagnostics
git add scripts/check_duplicates.py .githooks/pre-push .githooks/README.md
git commit -m "chore: improve jscpd diagnostics and make pre-push warning-only"
```

2) Chạy type checks (mypy). Nếu repo dùng mypy config, nó sẽ báo lỗi cần fix.

```powershell
# trong virtualenv (.venv)
.\.venv\Scripts\python.exe -m pip install -r requirements-dev.txt || echo "skip deps install"
.\.venv\Scripts\python.exe -m mypy zeta_vn || Write-Host "mypy returned non-zero"
```

3) Chạy unit tests (pytest)

```powershell
.\.venv\Scripts\python.exe -m pytest -q
```

4) Kiểm tra và chạy lại script duplicate check (smoke test)

```powershell
.\.venv\Scripts\python.exe .\scripts\check_duplicates.py --jscpd-only --report-dir .\reports\duplicates
```

Nếu bạn gặp lỗi `[WinError 2]` hoặc `npx` không tìm thấy, chạy thủ công lệnh in ra trong output `Running: ...`.

5) Tạo PR draft (tùy bạn dùng GitHub CLI hoặc web UI). Ví dụ dùng gh cli:

```powershell
gh pr create --fill --title "chore: improve duplicate checks diagnostics" --draft
```

6) Cleanups & follow-ups (manual / opt-in):
- Nếu bạn muốn loại bỏ `# ruff: noqa: C901` trong `scripts/check_duplicates.py`, cân nhắc tách thêm các helper hoặc cập nhật cấu hình Ruff (per-file-ignores) trong `pyproject.toml`.
- Nếu muốn, mình có thể tạo patch tách thêm hàm để loại bỏ noqa; yêu cầu tiếp theo sẽ thực hiện.

Ghi chú
- Mình không tự chạy `git commit` hay `gh pr create` vì không tự động thao tác repo remote; mình chỉ tạo file này với các lệnh rõ ràng để bạn hoặc CI chạy.
- Nếu muốn, mình có thể tự động tạo thêm một branch patch file (chỉ trong workspace) hoặc tách helpers tiếp để loại bỏ noqa — hãy cho biết nếu muốn mình thực hiện tiếp.

---
Generated by automated assistant to summarize safe next steps after Ruff fixes and jscpd diagnostics improvements.
